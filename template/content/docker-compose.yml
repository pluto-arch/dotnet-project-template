version: '3.4'

services:
  pluto.netcoretemplate.api:
    image: ${DOCKER_REGISTRY-}templateService
    container_name: templateService
    build:
      context: .
      dockerfile: src/PlutoNetCoreTemplate.API/Dockerfile
